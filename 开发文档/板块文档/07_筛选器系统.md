# 筛选器系统详细文档

## 模块概述
筛选器系统是整个仪表板的核心交互组件，采用电商式下拉选择器设计，支持多维度组合筛选、级联锁定、搜索过滤等高级功能，为五大分析板块提供精准的数据筛选能力。

## 核心功能架构

### 1. 筛选器分组体系
```javascript
const drillDownDimensions = [
    // Group 1: 核心组织
    { key: 'third_level_organization', label: '三级机构', group: 1 },
    
    // Group 2: 时间维度  
    { key: 'policy_start_year', label: '保单年度', group: 2 },
    { key: 'week_number', label: '周次', group: 2 },
    
    // Group 3: 业务核心
    { key: 'insurance_type', label: '险种', group: 3 },
    { key: 'ui_short_label', label: '业务类型', group: 3 },
    { key: 'coverage_type', label: '险别组合', group: 3 },
    
    // Group 4: 车辆属性
    { key: 'is_new_energy_vehicle', label: '是否新能源', group: 4 },
    { key: 'is_transferred_vehicle', label: '是否过户', group: 4 },
    { key: 'renewal_status', label: '续保状态', group: 4 },
    { key: 'vehicle_insurance_grade', label: '车险分等级', group: 4 },
    { key: 'small_truck_score', label: '小货车评分', group: 4 },
    { key: 'large_truck_score', label: '大货车评分', group: 4 },
    
    // Group 5: 渠道
    { key: 'terminal_source', label: '终端来源', group: 5 }
];
```

### 2. 状态管理系统
```javascript
filterState = {
    drill: {
        applied: [],    // 已应用的筛选条件 [{dimension, values}]
        draft: {}       // 草稿状态 {dimensionKey: [selectedValues]}
    }
};
```

## 组件设计

### 1. 筛选器按钮组件
```html
<div class="drill-selector-btn" data-dimension="third_level_organization">
    <span class="selector-label">三级机构</span>
    <span class="selection-count"></span>
    <svg class="dropdown-arrow">...</svg>
</div>
```

### 2. 下拉面板组件
```html
<div id="drill-dropdown-panel" class="drill-dropdown-panel">
    <!-- 搜索区域 -->
    <div class="drill-dropdown-search">
        <input type="text" id="drill-search-input" placeholder="搜索..." />
    </div>
    
    <!-- 操作按钮 -->
    <div class="drill-dropdown-actions">
        <button onclick="Dashboard.toggleAllInDropdown('all')">全选</button>
        <button onclick="Dashboard.toggleAllInDropdown('invert')">反选</button>
        <button onclick="Dashboard.toggleAllInDropdown('none')">清空</button>
        <button onclick="Dashboard.applyDrillFilters()" class="primary">确定</button>
    </div>
    
    <!-- 选项列表 -->
    <div class="drill-dropdown-list" id="drill-dropdown-list">
        <!-- 动态生成checkbox选项 -->
    </div>
</div>
```

## 交互逻辑

### 1. 筛选器打开/关闭
```javascript
openDropdown(dimensionKey, buttonElement) {
    // 关闭其他下拉面板
    if (this.activeDropdown && this.activeDropdown !== dimensionKey) {
        this.closeDropdown(this.activeDropdown);
    }
    
    // 设置当前激活的下拉面板
    this.activeDropdown = dimensionKey;
    
    // 定位下拉面板
    this.positionDropdown(buttonElement);
    
    // 加载选项数据
    this.loadDropdownOptions(dimensionKey);
    
    // 恢复草稿状态
    this.restoreDraftState(dimensionKey);
    
    // 显示面板
    document.getElementById('drill-dropdown-panel').style.display = 'block';
    
    // 聚焦搜索框
    document.getElementById('drill-search-input').focus();
}

closeDropdown(dimensionKey) {
    document.getElementById('drill-dropdown-panel').style.display = 'none';
    this.activeDropdown = null;
}
```

### 2. 选项选择逻辑
```javascript
toggleOption(dimensionKey, value, isChecked) {
    if (!this.filterState.drill.draft[dimensionKey]) {
        this.filterState.drift.draft[dimensionKey] = [];
    }
    
    const draftValues = this.filterState.drill.draft[dimensionKey];
    
    if (isChecked) {
        if (!draftValues.includes(value)) {
            draftValues.push(value);
        }
    } else {
        const index = draftValues.indexOf(value);
        if (index > -1) {
            draftValues.splice(index, 1);
        }
    }
    
    // 更新UI状态
    this.updateDropdownButtonUI(dimensionKey);
}

// 全选/反选/清空操作
toggleAllInDropdown(action) {
    const dimensionKey = this.activeDropdown;
    const allValues = this.getDimensionValues(dimensionKey);
    
    switch (action) {
        case 'all':
            this.filterState.drill.draft[dimensionKey] = [...allValues];
            break;
        case 'none':
            this.filterState.drill.draft[dimensionKey] = [];
            break;
        case 'invert':
            const currentValues = this.filterState.drill.draft[dimensionKey] || [];
            this.filterState.drill.draft[dimensionKey] = allValues.filter(v => !currentValues.includes(v));
            break;
    }
    
    this.updateDropdownCheckboxes(dimensionKey);
    this.updateDropdownButtonUI(dimensionKey);
}
```

### 3. 筛选条件应用
```javascript
async applyDrillFilters() {
    const appliedFilters = [];
    
    // 收集所有有选择值的维度
    for (const [dimensionKey, values] of Object.entries(this.filterState.drill.draft)) {
        if (values && values.length > 0) {
            appliedFilters.push({
                dimension: dimensionKey,
                values: [...values]
            });
        }
    }
    
    // 更新已应用的筛选条件
    this.filterState.drill.applied = appliedFilters;
    
    // 应用级联锁定
    this.applyCascadingLocks(appliedFilters);
    
    // 关闭下拉面板
    this.closeDropdown(this.activeDropdown);
    
    // 重新处理数据
    await this.reprocessData();
    
    // 更新所有图表
    this.refreshAllCharts();
    
    // 更新筛选器标签显示
    this.renderDrillTags();
}
```

## 级联锁定系统

### 1. 业务类型触发锁定
```javascript
applyCascadingLocks(appliedFilters) {
    // 清除之前的锁定
    this.clearAllLocks();
    
    // 获取业务类型选择
    const businessTypeFilter = appliedFilters.find(f => f.dimension === 'ui_short_label');
    if (!businessTypeFilter) return;
    
    const businessTypes = businessTypeFilter.values;
    
    // 规则A: 续保状态锁定
    const allNew = businessTypes.every(t => t.includes('新') && !t.includes('旧'));
    const hasOld = businessTypes.some(t => t.includes('非营客-旧'));
    
    if (allNew) {
        // 全部为新业务类型时，锁定续保状态为"新保"
        this.lockSelector('renewal_status', ['新保']);
    } else if (hasOld) {
        // 包含旧业务类型时，排除"新保"选项
        this._excludeNewRenewal = true;
    }
    
    // 规则B: 是否过户锁定
    const hasTransfer = businessTypes.some(t => t.includes('非营客-过户'));
    if (hasTransfer) {
        // 包含过户业务类型时，锁定为"是"
        this.lockSelector('is_transferred_vehicle', ['true']);
    }
}
```

### 2. 锁定机制实现
```javascript
lockSelector(dimensionKey, allowedValues) {
    // 记录锁定状态
    this._lockedSelectors = this._lockedSelectors || {};
    this._lockedSelectors[dimensionKey] = {
        allowedValues: allowedValues,
        reason: this.getLockReason(dimensionKey, allowedValues)
    };
    
    // 更新UI显示
    this.updateLockedSelectorUI(dimensionKey);
    
    // 如果当前选择的值不在允许范围内，自动调整
    const currentValues = this.filterState.drill.draft[dimensionKey] || [];
    const validValues = currentValues.filter(v => allowedValues.includes(v));
    
    if (validValues.length !== currentValues.length) {
        this.filterState.drill.draft[dimensionKey] = validValues;
        this.updateDropdownCheckboxes(dimensionKey);
    }
}

updateLockedSelectorUI(dimensionKey) {
    const button = document.querySelector(`.drill-selector-btn[data-dimension="${dimensionKey}"]`);
    const lock = this._lockedSelectors[dimensionKey];
    
    if (lock) {
        button.classList.add('locked');
        button.title = `已锁定: ${lock.reason}`;
        
        // 添加锁定图标
        if (!button.querySelector('.lock-icon')) {
            const icon = document.createElement('span');
            icon.className = 'lock-icon';
            icon.innerHTML = '🔒';
            button.appendChild(icon);
        }
    } else {
        button.classList.remove('locked');
        button.title = '';
        
        // 移除锁定图标
        const lockIcon = button.querySelector('.lock-icon');
        if (lockIcon) lockIcon.remove();
    }
}
```

## 搜索过滤系统

### 1. 实时搜索
```javascript
handleSearchInput(searchTerm) {
    const dimensionKey = this.activeDropdown;
    if (!dimensionKey) return;
    
    const allOptions = this.getDimensionValues(dimensionKey);
    const filteredOptions = this.filterOptions(allOptions, searchTerm);
    
    this.renderDropdownOptions(dimensionKey, filteredOptions);
    
    // 高亮搜索词
    this.highlightSearchTerms(searchTerm);
}

filterOptions(options, searchTerm) {
    if (!searchTerm) return options;
    
    const lowerSearchTerm = searchTerm.toLowerCase();
    
    return options.filter(option => {
        const displayValue = this.getValueLabel(dimensionKey, option).toLowerCase();
        return displayValue.includes(lowerSearchTerm);
    });
}

highlightSearchTerms(searchTerm) {
    if (!searchTerm) return;
    
    const optionElements = document.querySelectorAll('.drill-dropdown-option');
    optionElements.forEach(element => {
        const textElement = element.querySelector('.option-text');
        if (!textElement) return;
        
        const originalText = textElement.textContent;
        const regex = new RegExp(`(${searchTerm})`, 'gi');
        const highlightedText = originalText.replace(regex, '<mark>$1</mark>');
        
        textElement.innerHTML = highlightedText;
    });
}
```

### 2. 搜索性能优化
```javascript
// 防抖搜索
this.debouncedSearch = this.debounce((searchTerm) => {
    this.handleSearchInput(searchTerm);
}, 200);

// 搜索缓存
this._searchCache = new Map();

getCachedSearchResult(dimensionKey, searchTerm) {
    const cacheKey = `${dimensionKey}:${searchTerm}`;
    
    if (!this._searchCache.has(cacheKey)) {
        const result = this.performSearch(dimensionKey, searchTerm);
        this._searchCache.set(cacheKey, result);
    }
    
    return this._searchCache.get(cacheKey);
}
```

## UI状态管理

### 1. 按钮状态更新
```javascript
updateSelectorButtonUI(dimensionKey) {
    const btn = document.querySelector(`.drill-selector-btn[data-dimension="${dimensionKey}"]`);
    const draft = this.filterState.drill.draft[dimensionKey];
    const applied = this.filterState.drill.applied.find(c => c.dimension === dimensionKey)?.values;
    
    const values = draft !== undefined ? draft : (applied || []);
    const count = values.length;
    const dimensionLabel = this.getDrillDownDimensions().find(d => d.key === dimensionKey)?.label || '';
    
    const countSpan = btn.querySelector('.selection-count');
    const labelSpan = btn.querySelector('.selector-label');
    
    if (count === 0) {
        btn.classList.remove('has-selection');
        countSpan.textContent = '';
        labelSpan.textContent = dimensionLabel;
    } else if (count === 1) {
        btn.classList.add('has-selection');
        const displayVal = this.getValueLabel(dimensionKey, values[0]);
        countSpan.textContent = '';
        labelSpan.textContent = `${dimensionLabel}: ${displayVal}`;
    } else {
        btn.classList.add('has-selection');
        const firstDisplayVal = this.getValueLabel(dimensionKey, values[0]);
        countSpan.textContent = `+${count - 1}`;
        labelSpan.textContent = `${dimensionLabel}: ${firstDisplayVal}`;
    }
}
```

### 2. 标签显示系统
```javascript
renderDrillTags() {
    const tagsContainer = document.getElementById('drill-tags-container');
    if (!tagsContainer) return;
    
    tagsContainer.innerHTML = '';
    
    this.filterState.drill.applied.forEach(filter => {
        const dimensionInfo = this.getDrillDownDimensions().find(d => d.key === filter.dimension);
        if (!dimensionInfo) return;
        
        filter.values.forEach(value => {
            const tag = this.createDrillTag(filter.dimension, value, dimensionInfo.label);
            tagsContainer.appendChild(tag);
        });
    });
    
    // 添加清除全部按钮
    if (this.filterState.drill.applied.length > 0) {
        const clearAllBtn = this.createClearAllButton();
        tagsContainer.appendChild(clearAllBtn);
    }
}

createDrillTag(dimensionKey, value, dimensionLabel) {
    const tag = document.createElement('span');
    tag.className = 'drill-tag';
    
    const displayValue = this.getValueLabel(dimensionKey, value);
    
    tag.innerHTML = `
        <span class="tag-text">${dimensionLabel}: ${displayValue}</span>
        <span class="tag-remove" onclick="Dashboard.removeDrillTag('${dimensionKey}', '${value}')">×</span>
    `;
    
    return tag;
}
```

## 样式规范

### 1. 筛选器按钮样式
```css
.drill-selector-btn {
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: var(--border-radius-sm);
    padding: 8px 12px;
    min-width: 120px;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: relative;
}

.drill-selector-btn:hover {
    border-color: var(--primary-red);
    box-shadow: 0 2px 8px rgba(160, 39, 36, 0.1);
}

.drill-selector-btn.has-selection {
    background: rgba(160, 39, 36, 0.05);
    border-color: var(--primary-red);
}

.drill-selector-btn.locked {
    background: rgba(128, 128, 128, 0.1);
    border-color: #888888;
    cursor: not-allowed;
}

.dropdown-arrow {
    width: 16px;
    height: 16px;
    transition: transform 0.2s;
}

.drill-selector-btn.active .dropdown-arrow {
    transform: rotate(180deg);
}
```

### 2. 下拉面板样式
```css
.drill-dropdown-panel {
    position: absolute;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: var(--border-radius);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
    z-index: 1000;
    min-width: 280px;
    max-width: 400px;
    max-height: 400px;
    overflow: hidden;
}

.drill-dropdown-search {
    padding: 12px;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
}

.drill-dropdown-search input {
    width: 100%;
    padding: 8px 12px;
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: var(--border-radius-sm);
    font-size: 14px;
}

.drill-dropdown-actions {
    display: flex;
    gap: 8px;
    padding: 8px 12px;
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
}

.drill-action-btn {
    padding: 4px 12px;
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: var(--border-radius-sm);
    background: white;
    cursor: pointer;
    font-size: 12px;
    transition: var(--transition);
}

.drill-action-btn:hover {
    background: rgba(160, 39, 36, 0.05);
    border-color: var(--primary-red);
}

.drill-action-btn.primary {
    background: var(--primary-red);
    color: white;
    border-color: var(--primary-red);
}
```

### 3. 选项列表样式
```css
.drill-dropdown-list {
    max-height: 250px;
    overflow-y: auto;
    padding: 8px 0;
}

.drill-dropdown-option {
    display: flex;
    align-items: center;
    padding: 8px 12px;
    cursor: pointer;
    transition: var(--transition);
}

.drill-dropdown-option:hover {
    background: rgba(160, 39, 36, 0.05);
}

.drill-dropdown-option input[type="checkbox"] {
    margin-right: 8px;
}

.option-text {
    flex: 1;
    font-size: 14px;
}

.option-text mark {
    background: #ffeb3b;
    color: #333;
    padding: 0 2px;
}
```

## 性能优化

### 1. 虚拟滚动
```javascript
// 对于大量选项的虚拟滚动实现
renderVirtualizedOptions(dimensionKey, options) {
    const container = document.getElementById('drill-dropdown-list');
    const itemHeight = 32;
    const visibleCount = Math.ceil(container.clientHeight / itemHeight);
    const buffer = 5;
    
    return {
        items: options.slice(this.scrollTop, this.scrollTop + visibleCount + buffer),
        offset: this.scrollTop * itemHeight
    };
}
```

### 2. 数据缓存
```javascript
// 维度值缓存
this._dimensionValuesCache = new Map();

async getDimensionValues(dimensionKey) {
    if (!this._dimensionValuesCache.has(dimensionKey)) {
        const values = await this.worker.request('get_dimension_values', { dimension: dimensionKey });
        this._dimensionValuesCache.set(dimensionKey, values);
    }
    
    return this._dimensionValuesCache.get(dimensionKey);
}
```

## 扩展性设计

### 1. 新维度支持
```javascript
// 动态添加新维度
addDrillDownDimension(config) {
    drillDownDimensions.push({
        key: config.key,
        label: config.label,
        group: config.group,
        valueType: config.valueType || 'string',
        displayFormat: config.displayFormat || null
    });
}
```

### 2. 自定义锁定规则
```javascript
// 可配置的锁定规则系统
const lockRules = {
    businessType: {
        condition: (values) => values.includes('摩托车'),
        action: (dimensionKey) => this.lockSelector('vehicle_type', ['摩托车'])
    }
};
```

## 错误处理

### 1. 数据加载异常
```javascript
async loadDimensionValues(dimensionKey) {
    try {
        const values = await this.worker.request('get_dimension_values', { dimension: dimensionKey });
        return values || [];
    } catch (error) {
        console.error(`加载维度 ${dimensionKey} 数据失败:`, error);
        return [];
    }
}
```

### 2. 筛选异常处理
```javascript
async applyDrillFilters() {
    try {
        // 应用筛选逻辑
        await this.reprocessData();
    } catch (error) {
        console.error('应用筛选失败:', error);
        
        // 回退到之前的筛选状态
        this.rollbackFilterState();
        
        // 显示错误提示
        this.showErrorMessage('筛选条件应用失败，请重试');
    }
}
```

## 测试要点

### 1. 功能测试
- 验证筛选器打开/关闭交互
- 验证多选/单选逻辑正确性
- 验证搜索过滤功能
- 验证级联锁定机制

### 2. 性能测试
- 测试大量选项的渲染性能
- 测试搜索响应速度
- 测试频繁筛选操作的性能

### 3. 用户体验测试
- 验证按钮状态反馈
- 验证错误提示友好性
- 验证移动端适配效果